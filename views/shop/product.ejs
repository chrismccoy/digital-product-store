<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Purchase <%= product.name %> // <%= siteTitle %></title>
		<link href="/css/app.css" rel="stylesheet" />
	</head>
	<body class="bg-white text-black font-mono">
		<div class="max-w-7xl mx-auto p-4 sm:p-6 lg:p-8">
			<header
				class="flex justify-between items-center py-4 border-b-4 border-black"
			>
				<h1 class="text-2xl md:text-3xl font-bold uppercase">
					<a href="<%= homeURL %>" class="hover:bg-yellow-300"
						><%= homeText %></a
					>
				</h1>
			</header>

			<main>
				<section class="text-center py-12 md:py-16">
					<h2
						class="text-5xl md:text-7xl lg:text-8xl font-extrabold uppercase tracking-tighter"
					>
						<%= product.name %>
					</h2>
					<p class="max-w-2xl mx-auto mt-6 text-lg">
						Get instant access to the complete <%= product.text %>. One-time
						payment for lifetime access.
					</p>
				</section>

				<section
					id="payment-form"
					class="max-w-3xl mx-auto mt-24"
					data-price="<%= product.price %>"
					data-description="<%= product.text %>"
					data-product-id="<%= product.id %>"
				>
					<h3
						class="text-3xl font-bold uppercase inline-block bg-black text-yellow-300 px-4 py-2 mb-8"
					>
						// Secure Checkout
					</h3>
					<div class="border-2 border-black p-6 space-y-6">
						<div class="text-center pb-4">
							<p class="text-lg uppercase text-neutral-600">Total Due:</p>
							<p class="text-6xl font-extrabold text-black mt-2">
								$<%= parseFloat(product.price).toFixed(2) %>
							</p>
						</div>
						<div
							id="payment-messages"
							class="text-center text-red-600 font-bold"
						></div>
						<div id="paypal-button-container"></div>
						<p class="text-center text-sm text-neutral-500">
							You will be redirected to PayPal to complete your payment
							securely.
						</p>
					</div>
				</section>
			</main>

			<footer class="py-12 mt-16 border-t-4 border-black text-center">
				<p class="mt-12 text-sm text-neutral-600">
					&copy; 2025 <%= footerDomain %>. ALL RIGHTS RESERVED.
				</p>
			</footer>
		</div>

		<script
			src="https://www.paypal.com/sdk/js?client-id=<%= paypalClientId %>&currency=USD"
		></script>
		<script src="/js/shop.js"></script>
	</body>
</html>
