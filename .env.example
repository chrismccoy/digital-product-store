# --- Application Mode ---
# Set to 'shop' for the multi-product store.
# Set to 'single' for the streamlined single-product checkout.
APP_MODE=shop

# --- Shop Configuration ---
# The number of products to display per page in the shop.
ITEMS_PER_PAGE=6

# --- PayPal Credentials ---
# Obtain these from your PayPal Developer Dashboard (https://developer.paypal.com/).
# Use sandbox credentials for testing and development.
PAYPAL_CLIENT_ID=YOUR_SANDBOX_PAYPAL_CLIENT_ID
PAYPAL_CLIENT_SECRET=YOUR_SANDBOX_PAYPAL_CLIENT_SECRET
PAYPAL_API_MODE=sandbox # Use 'live' for production

# --- Server Configuration ---
# The port the application server will listen on.
PORT=3000

# --- Session Security ---
# A long, random, and secret string used to sign the session ID cookie.
# CRITICAL: Generate a new, secure string for production. Do not use this example.
SESSION_SECRET="generate-a-long-random-secure-string-for-this-12345-and-make-it-longer"

# --- Admin Credentials ---
# The username and password for logging into the /admin/dashboard.
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="change_this_password"

# --- Rate Limiting (for Admin Login) ---
# The time window for the rate limit in milliseconds.
AUTH_RATE_LIMIT_WINDOW_MS=900000 # 15 minutes
# The maximum number of FAILED login attempts allowed per window.
AUTH_RATE_LIMIT_MAX=5

# --- Site & Email Configuration ---
# These values are used in the EJS templates and in outgoing emails.
EMAIL_SUBJECT="Your Purchase Receipt & Download Link"
HOME_URL="/"
HOME_TEXT="Brutal Kit"
SITE_TITLE="Brutal Kit // Digital Goods"
CONTACT_URL="/contact/"
FOOTER_DOMAIN="BRUTALKIT.COM"

# --- SINGLE PRODUCT CONFIG (only used if APP_MODE=single) ---
SINGLE_PRODUCT_ID=brutal-ui-kit # The 'id' from products.json to use

# --- Email (Nodemailer) Configuration ---
# Choose ONE of the following methods for sending emails.

# Option 1: Use local sendmail (common on Linux servers, requires sendmail to be installed)
EMAIL_USE_SENDMAIL=true
SENDMAIL_PATH=/usr/sbin/sendmail # Adjust path if necessary for your system

# Option 2: Use an external SMTP server (e.g., Gmail, SendGrid, Mailgun)
# To use this, set EMAIL_USE_SENDMAIL=false and fill in the details below.
#EMAIL_HOST=smtp.example.com
#EMAIL_PORT=587
#EMAIL_USER=your_smtp_username
#EMAIL_PASS=your_smtp_password

# This is the "From" address that will appear in emails sent to customers.
EMAIL_FROM='"Your Store Name" <support@yourdomain.com>'

